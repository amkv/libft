везде должны быть unsigned char!!!!
там где это возможно использовать свои собственные функции
дописать еще +4 собственные функции
makefile изменить *.c
проверка на protected if (s && *s) -- можно ли только s ? (вероятнее всего, что да - и так правильнее)
почистить все файлы на norminette
скомпоновать файлы из temp и work - сохранить все проверочные main
static функции где функция в функции
проверить все функции на линейность объявлений и переменных

ft_putnbr --- другой вариант
--------------------
#AddressSanitizer
https://github.com/google/sanitizers/wiki/AddressSanitizer
https://en.wikipedia.org/wiki/AddressSanitizer

gcc -I. -fsanitize=address
./a.out
--------------------
nm ft*.o | grep U
nm ft*.o | grep T | wc -l
nm -u ./a.out

ar t libft.a
--------------------
Strequ
Strnequ

РАЗОБРАТЬ!
itoa
strsplit
strnstr

+ бонус
--------------------

# производит поиск в папке с именами ft_
# жждет ввода значения от 1 до ... (количество файлов ft_)
# 0 - последний использованный аргумент
# 
# выводит список файлов
# (1) ft_test.c
# (2) ft_desk.c
# ...

---------------------
ls -a | grep "ft_*" | sed 's/$/ \\/' > list.txt
---------------------
этапы проверки
собирается ли библиотека
количество файлов для проверки (все ли файлы сданы)
прописанные файлы в makefile
какие библиотеки использовались

---------------------
cp ft_ ~/t/libft
---------------------
// int COUNTER = 0;
// // printf("STEP: %d\n", ++COUNTER);
---------------------
gcc -Wall -Wextra -Wfloat-equal -Wundef -Werror -Wint-to-pointer-cast -Wshadow -Wpointer-arith -Wcast-align -Wstrict-prototypes -Wmissing-prototypes -Wstrict-overflow=5 -Wwrite-strings -Wconversion --pedantic-errors *.c

-Wmissing -prototypes
-Wstrict -prototypes



